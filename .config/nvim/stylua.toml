# Matches existing lua/** style; else conform's format-on-save would drift
# to stylua's defaults (4-space indent, double quotes).
indent_type = "Spaces"
indent_width = 2
quote_style = "AutoPreferSingle"
